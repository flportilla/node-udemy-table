# Notes:

First (no) app on node

```
Options:
      --help     Shows help                                           [boolean]
      --version  Show version number                                  [boolean]
  -b, --base     base for multiplication                    [number] [requerido]
  -l, --list     logs resulst on console              [boolean] [default: false]
  -u, --until    prints on the console until this number  [number] [default: 10]

  ```